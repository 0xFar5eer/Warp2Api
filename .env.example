# Warp2Api Environment Variables Configuration Example
# Copy this file to .env and modify the configuration as needed

# Warp2Api Configuration
# Set to true to enable verbose logging, default false (silent mode)
W2A_VERBOSE=false

# Server Configuration
HOST=0.0.0.0
PORT=8010

# Service Ports Configuration
OPENAI_COMPAT_PORT=4010
WARP_BRIDGE_PORT=8000
QDRANT_PORT=6333

# Bridge Server URL Configuration
WARP_BRIDGE_URL=http://127.0.0.1:28888

# OpenAI Compatibility URL (for KiloCode and other clients)
OPENAI_COMPAT_URL=http://localhost:4010

# API Key Authentication - Used to protect API endpoints
# If API_KEY is set, only requests with matching API key are allowed
# If API_KEY is not set (commented out), all requests are allowed
# API key can be provided via X-API-Key header or api_key query parameter
API_KEY=super_secure_key

# Legacy API Token (deprecated - use API_KEY instead)
# API_TOKEN=001

# Warp Authentication Related
# Refresh token obtained from the Warp application
WARP_REFRESH_TOKEN=your_warp_refresh_token_here

# JWT token (optional, usually obtained automatically)
# WARP_JWT=your_warp_jwt_token_here

# Network Proxy Configuration
# IMPORTANT: These settings are used internally by Docker and never exposed externally
#
# Option 1: Direct HTTP_PROXY URL (if your proxy provider gives you a complete URL)
# HTTP_PROXY=http://username:password@proxy.example.com:8080
# HTTPS_PROXY=http://username:password@proxy.example.com:8080
#
# Option 2: Separate proxy credentials (for rotating IP proxy services)
# The proxy will rotate IP address on each request for better anonymity
# These are combined internally: http://${PROXY_USER}:${PROXY_PASS}@${PROXY_HOST}:${PROXY_PORT}
# PROXY_USER=your_proxy_username
# PROXY_PASS=your_proxy_password
# PROXY_HOST=proxy.example.com
# PROXY_PORT=8080
#
# Never proxy localhost connections
NO_PROXY=127.0.0.1,localhost

# Logging Configuration
# Current settings (verbose - shows all info and access logs)
WARP_LOG_LEVEL=info
WARP_ACCESS_LOG=true
OPENAI_LOG_LEVEL=info
OPENAI_ACCESS_LOG=true

# Alternative settings for production (errors only, no access logs)
# Uncomment these lines and comment out the above to show only errors:
# WARP_LOG_LEVEL=error
# WARP_ACCESS_LOG=false
# OPENAI_LOG_LEVEL=error
# OPENAI_ACCESS_LOG=false

# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. API_KEY can be left empty for no authentication or set to a secure key
# 3. WARP_REFRESH_TOKEN needs to be obtained from the Warp application (optional)
# 4. Adjust other configurations as needed